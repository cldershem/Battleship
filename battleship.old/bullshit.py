#def checkGuess(guessCoords, turn):
    #guess_row = guessCoords[0]-1
    #guess_col = guessCoords[1]-1
    #loop through number of ships
        #if guess is correct
	    #print won
            #return won
	#if this is the last turn
	    #mark guess on map
            #mark ships on map
            #print answer
            #game over
	#else:
	    #if guess outside of board
		#print outside ocean
	    #if already guessed
		#print already guessed
	    #else:
		#you missed
                #mark guess on map
	    #print turns left
            #print board
            #break loop
